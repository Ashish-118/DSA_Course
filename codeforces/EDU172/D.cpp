#include <bits/stdc++.h>
#define int long long
using namespace std;

int32_t main()
{

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        pair<int, int> p;
        for (int i = 0; i < n; i++)
        {
            cin >> p.first >> p.second;
        }
        }

    return 0;
}